# Скопируйте в .env и заполните значения (файл .env в git не коммитится)
MAX_BOT_TOKEN=
ADMIN_CHAT_ID=
# Раньше пороги для детальных MAX-напоминаний; утренняя сводка MAX теперь всегда полная по журналу.
NOTIFY_DAYS_1=60
NOTIFY_DAYS_2=7

# SMTP и email администратора — в приложении: боковая панель → 📨 Email-уведомления
# (хранятся в таблице settings, не в .env)
